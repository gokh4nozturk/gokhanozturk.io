/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  plugins: [
    [
      {
        // Add any options here
      },
    ],
  ],
};

module.exports = nextConfig;
